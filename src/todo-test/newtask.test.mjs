import { assert } from "chai"
import DOM from "@testing-library/dom"

import newTaskEl, {template as newTaskTmpl} from "../templates/new-task.js"

describe("New Task", () => {
  it("should render a h3 title with value equal to Add Task")
})
